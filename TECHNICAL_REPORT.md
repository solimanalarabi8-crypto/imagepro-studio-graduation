# 📋 التقرير التقني الشامل لنظام ImagePro Studio
### توثيق تفصيلي لكافة أجزاء النظام، التقنيات الهندسية، والمكتبات البرمجية المستخدمة
> **مشروع تخرج لنيل درجة البكالوريوس في علوم الحاسوب وتكنولوجيا المعلومات**  
> **تخصص:** معالجة الصور الرقمية والرؤية الحاسوبية (Digital Image Processing & Computer Vision)  
> **إعداد الطلاب:**  
> • **سليمان العربي**  
> • **يعقوب المهاجري**  
> • **مالك عادل**  
> **تاريخ التوثيق:** 2026  

---

## 📑 فهرس التقرير
1. [المقدمة والملخص التنفيذي](#1-المقدمة-والملخص-التنفيذي)
2. [المعمارية العامة للنظام (System Architecture)](#2-المعمارية-العامة-للنظام-system-architecture)
3. [الجدول الشامل لكافة المكاتب والاعتماديات (Dependencies Inventory)](#3-الجدول-الشامل-لكافة-المكاتب-والاعتماديات-dependencies-inventory)
4. [تفصيل أجزاء النظام والتقنيات والمكتبات المستخدمة في كل جزء](#4-تفصيل-أجزاء-النظام-والتقنيات-والمكتبات-المستخدمة-في-كل-جزء)
   * [أولاً: بيئة العمل والبناء الأساسية (Core Runtime & Build System)](#أولاً-بيئة-العمل-والبناء-الأساسية-core-runtime--build-system)
   * [ثانياً: واجهة المستخدم وتجربة الاستخدام (UI/UX Layer)](#ثانياً-واجهة-المستخدم-وتجربة-الاستخدام-uiux-layer)
   * [ثالثاً: محرك الرسم البكسلي والكانفاس الأساسي (Canvas Core Engine)](#ثالثاً-محرك-الرسم-البكسلي-والكانفاس-الأساسي-canvas-core-engine)
   * [رابعاً: محرك الفلاتر والالتفاف الرياضي (Filters & Convolution Engine)](#رابعاً-محرك-الفلاتر-والالتفاف-الرياضي-filters--convolution-engine)
   * [خامساً: محرك الإضاءة والألوان والمدرج التكراري (Color Engine & Histogram)](#خامساً-محرك-الإضاءة-والألوان-والمدرج-التكراري-color-engine--histogram)
   * [سادساً: محرك الرسم الرقمي والأشكال الهندسية (Drawing & Shapes Engine)](#سادساً-محرك-الرسم-الرقمي-والأشكال-الهندسية-drawing--shapes-engine)
   * [سابعاً: حزمة التنقيح وإزالة العيوب (Retouching & Healing Engine)](#سابعاً-حزمة-التنقيح-وإزالة-العيوب-retouching--healing-engine)
   * [ثامناً: استوديو الذكاء الاصطناعي والشبكات العصبية (AI Neural Engine)](#ثامناً-استوديو-الذكاء-الاصطناعي-والشبكات-العصبية-ai-neural-engine)
   * [تاسعاً: نظام الطبقات المتقدم والسجل (Layers & Non-Destructive History)](#تاسعاً-نظام-الطبقات-المتقدم-والسجل-layers--non-destructive-history)
   * [عاشراً: محرك النصوص والطباعة (Typography Engine)](#عاشراً-محرك-النصوص-والطباعة-typography-engine)
   * [حادي عشر: محرك إدارة المشاريع والتصدير (Persistence & Export Engine)](#حادي-عشر-محرك-إدارة-المشاريع-والتصدير-persistence--export-engine)
   * [ثاني عشر: خادم الواجهة الخلفية والشبكة (Backend & API Server)](#ثاني-عشر-خادم-الواجهة-الخلفية-والشبكة-backend--api-server)
   * [ثالث عشر: بيئة الاختبارات وضمان الجودة (Testing & Quality Assurance)](#ثالث-عشر-بيئة-الاختبارات-وضمان-الجودة-testing--quality-assurance)
5. [المقارنة التقنية بين المعالجة في المتصفح وعلى الخادم](#5-المقارنة-التقنية-بين-المعالجة-في-المتصفح-وعلى-الخادم)
6. [الخاتمة والخلاصة الهندسية](#6-الخاتمة-والخلاصة-الهندسية)

---

## 1. المقدمة والملخص التنفيذي
نظام **ImagePro Studio** هو منصة برمجية هندسية متكاملة لتحرير ومعالجة الصور الرقمية وتطبيق خوارزميات الرؤية الحاسوبية والذكاء الاصطناعي مباشرة عبر الويب. تم بناء النظام ليعمل بكفاءة فائقة تعتمد على المعالجة الرياضية النقطية المباشرة (Pixel-level manipulation) دون الاعتماد على مكاتب جاهزة مسبقة الصنع لتطبيق المؤثرات الأساسية، بل تم بناء محركات معالجة رياضية مخصصة من الصفر لكل من الفلاتر، التعديلات الضوئية، الرسم، والتنقيح.

يتكامل النظام مع شبكات عصبية عميقة (Deep Neural Networks) لمعالجة المهام المعقدة كالتجزئة الدلالية وفصل العناصر (Image Matting) عبر نموذج **IS-Net** المتطور.

---

## 2. المعمارية العامة للنظام (System Architecture)
يعتمد النظام على **معمارية هجينة متعددة الطبقات (Hybrid Multi-Tier Architecture)** تضمن أعلى سرعة استجابة للمستخدم مع توفير أقصى قوة حسابية:

```
+-----------------------------------------------------------------------------------+
|                        1. طبقة واجهة المستخدم وتجربة الاستخدام                     |
|                 React 19 + Tailwind CSS v4 + Radix UI + Lucide                    |
+-----------------------------------------------------------------------------------+
                                         │
                                         ▼
+-----------------------------------------------------------------------------------+
|                     2. طبقة إدارة الحالة وتدفق البيانات (State)                   |
|           Canvas Context + Tool Registry + Layers Stack + History Memento         |
+-----------------------------------------------------------------------------------+
        │                                                     │
        ▼                                                     ▼
+------------------------------------+   +------------------------------------------+
|  3. محركات المعالجة المحلية للعميل |   |     4. الواجهة الخلفية والذكاء الاصطناعي |
|     (Client-side Web Engines)      |   |            (Node.js / Express API)       |
| • محرك الكانفاس (HTML5 Canvas 2D)  |   | • خادم Express 4.21                      |
| • محرك الفلاتر (Convolution 3x3)   |   | • مكتبة Sharp (C++ Libvips)              |
| • محرك الإضاءة والهستوجرام الحسابي |   | • نموذج IS-Net العصبي (ONNX Runtime)     |
| • محرك التنعيم التربيعي للفرشاة    |   | • إزالة الخلفية الذكية بدقة فائقة        |
| • خوارزميات التنقيح (Bilateral/IDW)|   |                                          |
+------------------------------------+   +------------------------------------------+
                                         │
                                         ▼
+-----------------------------------------------------------------------------------+
|                     5. طبقة التخزين والتصدير (Persistence & Export)               |
|      ملفات المشاريع (.imagepro) + تصدير متعدد المقاييس (0.5x, 1x, 2x) PNG/JPG/WebP |
+-----------------------------------------------------------------------------------+
```

---

## 3. الجدول الشامل لكافة المكاتب والاعتماديات (Dependencies Inventory)
يوضح هذا الجدول كافة المكتبات المسجلة في المشروع من ملف `package.json`، إصداراتها، والدور الهندسي الفعلي لكل مكتبة في النظام:

### أ. مكاتب التشغيل الأساسية (Production Dependencies)
| المكتبة (Library) | الإصدار | التصنيف | الدور الهندسي داخل النظام |
|---|:---:|:---:|---|
| **`react`** | `^19.2.1` | Core Framework | إطار العمل الأساسي لبناء واجهة المستخدم وتحديث شجرة العناصر التفاعلية. |
| **`react-dom`** | `^19.2.1` | Core Framework | ربط مكونات React بشجرة DOM الحقيقية للمتصفح. |
| **`@imgly/background-removal`** | `^1.7.0` | AI / Computer Vision | تشغيل نموذج الذكاء الاصطناعي IS-Net في المتصفح عبر WebAssembly و Web Workers. |
| **`@imgly/background-removal-node`** | `^1.4.5` | AI / Computer Vision | تشغيل نموذج الذكاء الاصطناعي على السيرفر بالاعتماد على محرك ONNX Runtime C++. |
| **`sharp`** | `^0.33.x` | High-Perf Image Processing | مكتبة C++ مدمجة بالسيرفر لمعالجة وتدوير وتقطيع ومزج الصور وإدارة مناطق ROI بسرعة فائقة. |
| **`@xenova/transformers`** | `^2.17.2` | Machine Learning | بنية تشغيل نماذج Hugging Face ومحولات الرؤية الحاسوبية في بيئة Node و JavaScript. |
| **`@google/genai`** | `^2.22.0` | AI Integration | واجهة برمجية للربط مع نماذج الذكاء الاصطناعي التوليدي عند الحاجة. |
| **`express`** | `^4.21.2` | Backend Server | خادم الويب الأساسي لخدمة واجهات برمجة التطبيقات (API) وملفات النظام الثابتة. |
| **`axios`** | `^1.12.0` | Networking | إدارة طلبات HTTP بين العميل وخادم المعالجة لنقل الصور والبيانات المعالجة. |
| **`lucide-react`** | `^0.453.0` | UI Icons | توفير كافة الأيقونات المتجهة لأدوات النظام، القوائم، والأزرار. |
| **`framer-motion`** | `^12.23.22` | UI Animation | تحريك اللوحات والنوافذ المنبثقة وانتقالات الحالة بسلاسة فيزيائية 60FPS. |
| **`recharts`** | `^2.15.2` | Data Visualization | رسم بياني عالي الدقة لعرض الهيستوجرام وتوزيع قنوات الألوان (R, G, B, Luminance). |
| **`sonner`** | `^2.0.7` | UI Notifications | نظام الإشعارات والتنبيهات المباشرة (Toast) لإعلام المستخدم بنجاح العمليات. |
| **`wouter`** | `^3.3.5` | Routing | موجه خفيف وسريع جداً لإدارة مسارات وتوجيه صفحات التطبيق. |
| **`zod`** | `^4.1.12` | Validation | فحص وتدقيق وتأكيد صحة البيانات وأنماط كائنات المشاريع وملفات الإعدادات. |
| **`clsx`** | `^2.1.1` | Styling Utility | دمج وتطبيق فئات CSS الشرطية بكفاءة عالية. |
| **`tailwind-merge`** | `^3.3.1` | Styling Utility | حل تعارضات فئات Tailwind CSS تلقائياً عند دمج الأنماط البرمجية. |
| **`class-variance-authority`** | `^0.7.1` | Component Variants | إنشاء وتحديد أنماط وأحجام مختلفة لمكونات الواجهة بنظام Type-Safe. |
| **`nanoid`** | `3.3.7` (Override) | Utility | توليد معرفات فريدة (Unique IDs) للطبقات، الخطوات، وعناصر الرسم. |
| **`next-themes`** | `^0.4.6` | Theme Provider | إدارة سمات الواجهة ودعم الوضع الليلي المتطور (Dark Mode). |
| **`react-hook-form`** | `^7.64.0` | Form Management | إدارة حقول الإدخال والتحكم في إعدادات النوافذ والتصدير بأعلى أداء. |
| **`@hookform/resolvers`** | `^5.2.2` | Form Validation | ربط نماذج react-hook-form مع مكتبة التحقق Zod. |
| **`react-resizable-panels`** | `^3.0.6` | Layout Control | دعم التحكم في أحجام وتوزيع اللوحات الجانبية ومساحة العمل بالسحب. |
| **`embla-carousel-react`** | `^8.6.0` | UI Carousel | عرض الصور النموذجية والتنقل بين المعارض بنعومة. |
| **`cmdk`** | `^1.1.1` | Command Palette | لوحة أوامر بحثية سريعة شبيهة بنظام Spotlight / Command+K. |
| **`vaul`** | `^1.1.2` | Drawer Primitive | توفير أدراج سحب سفلية تفاعلية للشاشات والأجهزة اللمسية. |

### ب. مكاتب مكونات الواجهة القياسية (Radix UI Primitives)
استُخدمت حزمة **Radix UI** الكاملة لضمان أعلى مستويات إمكانية الوصول (Accessibility - a11y) والتوافق مع قارئات الشاشة واختصارات لوحة المفاتيح:
* `@radix-ui/react-slider`: سلايدرات التحكم في السطوع والتباين وحجم الفرشاة والشفافية.
* `@radix-ui/react-tabs`: تبويبات لوحة الخصائص (الخصائص، التعديلات، الفلاتر، الطبقات، السجل).
* `@radix-ui/react-dialog`: النوافذ المنبثقة (مشروع جديد، التصدير المخصص، دليل المستخدم).
* `@radix-ui/react-dropdown-menu`: القوائم المنسدلة في الشريط العلوي (ملف، تحرير، صورة، مرشح، عرض).
* `@radix-ui/react-menubar`: شريط القوائم الرئيسي بكافة مفاتيحه التفاعلية.
* `@radix-ui/react-tooltip`: تلميحات الشرح التي تظهر عند تمرير الفأرة فوق كل أداة واختصارها.
* `@radix-ui/react-context-menu`: القائمة السياقية عند النقر بزر الفأرة الأيمن على لوح العمل أو الطبقات.
* `@radix-ui/react-select`: القوائم المنسدلة لاختيار الخطوط العربية والإنجليزية وأوضاع المزج.
* `@radix-ui/react-checkbox`: مربعات التفعيل لتثبيت نسبة العرض للارتفاع وعرض الشبكة.
* `@radix-ui/react-switch`: مفاتيح التبديل السريعة لتفعيل المؤثرات الحية.
* `@radix-ui/react-popover`: نوافذ منبثقة لاختيار الألوان وضبط الإعدادات الدقيقة.
* `@radix-ui/react-scroll-area`: شريط تمرير ناعم ومخصص داخل لوحات الأدوات والطبقات.
* `@radix-ui/react-accordion`: الأقسام القابلة للطي في لوحة الفلاتر والتعديلات المتقدمة.
* `@radix-ui/react-alert-dialog`: رسائل التحذير التأكيدية عند حذف طبقة أو تفريغ العمل.
* `@radix-ui/react-progress`: أشرطة التقدم الحية عند تنزيل نموذج الذكاء الاصطناعي أو التصدير.
* `@radix-ui/react-radio-group`: أزرار الاختيار الدائرية لاختيار صيغ التصدير (PNG, JPG, WebP).
* `@radix-ui/react-separator`: الفواصل البصرية الأنيقة بين مجموعات الأدوات.
* `@radix-ui/react-toggle` و `@radix-ui/react-toggle-group`: أزرار تبديل أداة التحديد وأشكال التعبئة.

### ج. أدوات التطوير والبناء والاختبار (DevDependencies)
| الأداة | الإصدار | الوظيفة الهندسية |
|---|:---:|---|
| **`vite`** | `^7.1.7` | مجمع وخادم التطوير فائق السرعة المزود بميزة HMR (Hot Module Replacement). |
| **`typescript`** | `5.6.3` | المترجم البرمجي لضمان سلامة الأنواع ومنع الأخطاء البرمجية أثناء كتابة الكود. |
| **`vitest`** | `^2.1.4` | إطار الاختبارات الآلية فائقة السرعة المخصص لاختبار خوارزميات ومحركات المعالجة. |
| **`esbuild`** | `^0.25.0` | مجمع فائق السرعة مكتوب بلغة Go لتجميع كود السيرفر وتحويله إلى حزمة إنتاجية سريعة. |
| **`tailwindcss`** | `^4.1.14` | محرك التنسيقات الحديث عالي الأداء لبناء الواجهة الداكنة بدقة بكسلية. |
| **`@tailwindcss/vite`** | `^4.1.3` | إضافة Vite الرسمية لتكامل مباشر وبناء فوري لأنماط Tailwind v4. |
| **`postcss`** و **`autoprefixer`** | أحدث إصدار | معالجة ملفات CSS وإضافة البادئات المتوافقة مع كافة المتصفحات تلقائياً. |
| **`prettier`** | `^3.6.2` | التنسيق الآلي المعياري لكافة ملفات الكود وفق معايير النظافة البرمجية. |
| **`tsx`** | `^4.19.1` | مشغل سريع لتنفيذ ملفات TypeScript على Node.js مباشرة دون انتظار التجميع. |

---

## 4. تفصيل أجزاء النظام والتقنيات والمكتبات المستخدمة في كل جزء

### أولاً: بيئة العمل والبناء الأساسية (Core Runtime & Build System)
1. **التقنيات المستخدمة:**
   * **Node.js (ES Modules):** بيئة التشغيل الأساسية المبنية على معيار الوحدات الحديث (`"type": "module"`).
   * **TypeScript 5.6:** بيئة التحقق الصارم من الأنواع (Strict Type Checking) مع واجهات برمجية محددة لكل كائن في النظام.
   * **Vite 7:** خادم تطوير متقدم يعتمد على الـ Native ESM لتحديث الواجهة في أجزاء من الثانية.
2. **المكتبات المشاركة:** `vite`, `typescript`, `esbuild`, `tsx`.
3. **الملفات المسؤولة:**
   * `package.json`
   * `tsconfig.json`
   * `vite.config.ts`

---

### ثانياً: واجهة المستخدم وتجربة الاستخدام (UI/UX Layer)
1. **التقنيات المستخدمة:**
   * **React 19 (Hooks & Functional Components):** إدارة دورة حياة المكونات وحالات الواجهة (`useState`, `useEffect`, `useRef`, `useCallback`, `useMemo`).
   * **نمط تصميم "استوديو الفحم والفيروز":** ألوان HSL مخصصة (الخلفية الفحمية الداكنة `#0d1413` مع اللون الفيروزي النشط `#2DD4BF`).
   * **خطوط الويب العربية واللاتينية:** دمج خطوط احترافية مثل **IBM Plex Sans Arabic** و **Cairo** و **Space Grotesk**.
2. **المكتبات المشاركة:**
   * حزمة `@radix-ui/react-*` لبناء كافة القوائم والسلايدرات والمودالات.
   * `lucide-react` لجميع الأيقونات البصرية.
   * `framer-motion` للتحريكات والانتقالات.
   * `sonner` لنظام رسائل الحالة والإشعارات اللحظية.
   * `clsx` و `tailwind-merge` لدمج الأنماط.
3. **الملفات المسؤولة:**
   * `client/src/pages/Home.tsx`: صفحة العمل الرئيسية التي تجمع كافة الألواح والكانفاس.
   * `client/src/components/UserGuide.tsx`: المكون التفاعلي المدمج لدليل المستخدم الشامل.
   * `client/src/components/ui/*`: مجلد يضم أكثر من 50 مكون واجهة قياسي وقابل لإعادة الاستخدام.

---

### ثالثاً: محرك الرسم البكسلي والكانفاس الأساسي (Canvas Core Engine)
1. **التقنيات المستخدمة:**
   * **HTML5 Canvas 2D Context API:** الرسم المباشر والوصول للبكسلات عبر كائنات `ImageData`.
   * **مصفوفات البكسل المباشرة (Uint8ClampedArray):** الوصول المباشر لمصفوفة البكسلات أحادية البعد ذات النمط المتكرر `[R, G, B, A, R, G, B, A, ...]`.
   * **المصفوفات الرياضية ثنائية الأبعاد:** تحويل الإحداثيات من فضاء الشاشة (Screen Coordinates) إلى فضاء الصورة الحقيقي (Image Coordinates) وفق مصفوفة التحويل الأفيني (Affine Transform Matrix) المتأثرة بنسبة التكبير والإزاحة.
2. **الملفات المسؤولة:**
   * `client/src/pages/Home.tsx` (قسم معالجة أحداث الفأرة والكانفاس).
   * `client/src/lib/selection.ts`: محرك التحديد الهندسي (مستطيل، بيضاوي، استبدال، إضافة، طرح).

---

### رابعاً: محرك الفلاتر والالتفاف الرياضي (Filters & Convolution Engine)
1. **التقنيات والخوارزميات المستخدمة:**
   * **خوارزمية الالتفاف المكاني (Spatial 2D Convolution):** تطبيق نوى رياضية (Convolution Kernels) بمصفوفات $3 \times 3$ و $5 \times 5$ لحساب القيم الجديدة لكل بكسل بناءً على جيرانه.
   * **خوارزميات كشف الحواف:**
     * **فلتر Sobel:** حساب التدرج الرياضي في الاتجاهين الأفقي $G_x$ والرأسي $G_y$ عبر المعادلة:  
       $$G = \sqrt{G_x^2 + G_y^2}$$
     * **فلتر Canny:** تنعيم جاوسي متبوع بحساب التدرج وقمع النقاط غير العظمى (Non-maximum Suppression) مع عتبة مزدوجة (Hysteresis Thresholding).
     * **فلتر Laplacian:** حساب المشتقة الثانية لكشف تغيرات التردد العالي.
     * **نقش بارز (Emboss):** مصفوفة تدرج مائل مع إزاحة ضوئية محايدة (128).
   * **فلاتر التمويه والحدة:**
     * **Gaussian Blur:** توزيع جاوسي ثنائي الأبعاد لحساب أوزان التنعيم.
     * **Unsharp Masking:** طرح الصورة المموهة من الأصلية لإنشاء قناع حدة مجهري.
   * **الفلاتر الفنية والهندسية:**
     * **Median Filter:** فرز مصفوفة الجيران واختيار الوسيط لإزالة شوائب الملح والفلفل دون تشويه الحواف.
     * **Bilateral Filter:** تنعيم يجمع بين القرب المكاني وتشابه الشدة اللونية لحفظ الحواف الحادة.
     * **Pixelate & Posterize:** تقليص التردد المكاني وتكميم المستويات اللونية (Color Quantization).
     * **Oil Painting:** محاكاة ضربات الفرشاة الفنية عبر حساب الهيستوجرام المحلي للشدة وتطبيق اللون الأكثر تكراراً.
2. **المكتبات المشاركة:** محرك كودي مستقل ومكتوب بالكامل بـ TypeScript بدون مكاتب خارجية ثقيلة لضمان أعلى أداء.
3. **الملفات المسؤولة:**
   * `client/src/lib/filters-engine.ts`
   * `client/src/lib/filters-engine.test.ts`

---

### خامساً: محرك الإضاءة والألوان والمدرج التكراري (Color Engine & Histogram)
1. **التقنيات والخوارزميات المستخدمة:**
   * **التحويلات الرياضية النقطية (Per-Pixel Color Transforms):**
     * **السطوع والتباين:** معادلة التباين الخطية $C' = (C - 128) \times \text{factor} + 128 + \text{brightness}$.
     * **فضاء الألوان HSL:** تحويل فضاء RGB إلى HSL لإجراء دوران زاوية اللون (Hue Rotation) وتعديل التشبع بدقة، ثم إعادة التحويل إلى RGB.
     * **تصحيح جاما (Gamma Correction):** تصحيح أسي غير خطي:  
       $$V_{\text{out}} = 255 \times \left(\frac{V_{\text{in}}}{255}\right)^\gamma$$
     * **حرارة اللون (Color Temperature):** موازنة القنوات اللونية لزيادة الدفء أو البرودة.
     * **العتبة الثنائية (Binary Thresholding):** تحويل المشهد إلى أبيض وأسود نقي عند حد فاصل محدد.
   * **المدرج التكراري الحي (Real-time Histogram):** خوارزمية تمرير أحادي (Single-pass algorithm) تقوم بجدولة 256 مستوى تكراري لكل من القنوات (الأحمر، الأخضر، الأزرق، والإضاءة الكلية) وتحديثها فورياً أثناء السحب.
2. **المكتبات المشاركة:** `recharts` لعرض المدرج التكراري، مع كود معالجة بكسلي مخصص.
3. **الملفات المسؤولة:**
   * `client/src/lib/color-adjustments.ts`
   * `client/src/lib/color-adjustments.test.ts`

---

### سادساً: محرك الرسم الرقمي والأشكال الهندسية (Drawing & Shapes Engine)
1. **التقنيات والخوارزميات المستخدمة:**
   * **خوارزمية التنعيم بالمنحنيات التربيعية (Quadratic Bezier Midpoint Smoothing):** التقاط نقاط حركة الفأرة المتتالية وحساب نقاط المنتصف (Midpoints) ورسم منحنيات تربيعية ناعمة بينها لإلغاء أي رعشة يدوية.
   * **خوارزمية الملء الفيضاني (BFS Flood Fill):** خوارزمية البحث في العرض رباعية الاتجاهات (4-Way Breadth-First Search) لملء المساحات المتصلة بلون محدد استناداً إلى مسافة التفاوت اللوني الإقليدية في الفضاء ثلاثي الأبعاد RGB:  
     $$\Delta E = \sqrt{(R_1 - R_2)^2 + (G_1 - G_2)^2 + (B_1 - B_2)^2} \le \text{Tolerance}$$
   * **محرك المتجهات للأشكال:** رسم الأشكال الهندسية (مستطيل، بيضاوي، خط، مثلث، نجمة خماسية) مع دعم أنماط الإطار والتعبئة.
2. **المكتبات المشاركة:** كود حسابي مستقل على الـ 2D Canvas.
3. **الملفات المسؤولة:**
   * `client/src/lib/drawing-engine.ts`
   * `client/src/lib/drawing-engine.test.ts`

---

### سابعاً: حزمة التنقيح وإزالة العيوب (Retouching & Healing Engine)
1. **التقنيات والخوارزميات المستخدمة:**
   * **ختم الاستنساخ (Clone Stamp):** نسخ بكسلي متدرج من نقطة مصدر $(S_x, S_y)$ إلى نقطة هدف $(T_x, T_y)$ مع دالة ترييش جيب التمام الناعمة (Cosine Falloff Weighting).
   * **فرشاة المعالجة (Healing Brush):** استخراج النسيج عالي التردد من المصدر ودمجه مع إضاءة ولون المنطقة الهدف عبر مطابقة الإضاءة (Luminance Matching).
   * **معالجة البقع الفورية (Spot Healing):** أخذ عينات دائرية من محيط البقعة ($0.85r - 1.25r$) ونشرها نحو المركز بوزن مقلوب مربع المسافة (Inverse Distance Weighting - IDW).
   * **إزالة العين الحمراء (Red-Eye Removal):** كشف شرط الاحمرار الزائد ($R > 1.25 \times \max(G, B)$) وقمع القناة الحمراء إلى تدرج رمادي طبيعي مع صيانة بريق العين المنعكس (Specular Highlights).
   * **تنعيم البشرة الذكي (Smart Skin Smoothing):** مرشح ثنائي الاتجاه (Bilateral Filter) يعمل حصراً داخل نطاق تدرجات ألوان البشرة (Skin Gamut) لحماية تفاصيل العينين والشعر والشفاه.
   * **الملء المحيطي الذكي (Content-Aware Inpaint Fill):** نشر أنسجة الإطار المحيط المتصل لملء المساحات المفرغة بانسجام.
2. **الملفات المسؤولة:**
   * `client/src/lib/retouch-engine.ts`
   * `client/src/lib/retouch-engine.test.ts`

---

### ثامناً: استوديو الذكاء الاصطناعي والشبكات العصبية (AI Neural Engine)
1. **التقنيات ونماذج التعلم العميق المستخدمة:**
   * **نموذج IS-Net (Dichotomous Image Segmentation):** شبكة عصبية تلافيفية عميقة مصممة خصيصاً للتجزئة فائقة الدقة للأجسام وتحديد حدود ألفا الناعمة (Soft Alpha Matte).
   * **معمارية المعالجة المزدوجة (Dual-Execution Architecture):**
     1. **معالجة على العميل (Client-Side):** استخدام WebAssembly مع Web Workers عبر مكتبة `@imgly/background-removal` لمعالجة الصور مباشرة داخل المتصفح دون استهلاك خادم الويب.
     2. **معالجة على الخادم (Server-Side Fallback & ROI):** استخدام مكتبة `@imgly/background-removal-node` المدعومة بمحرك **ONNX Runtime C++ Native** على خادم Node.js مع مكتبة **Sharp** لمعالجة الصور الكبيرة أو تحديد مناطق اهتمام معينة (Region of Interest - ROI) وتجاوز قيود متصفحات الويب وأمان CORS.
   * **تمويه البورتريه الذكي (Portrait Bokeh):** عزل الجسم كقناع ثنائي، تطبيق تمويه جاوسي على طبقة الخلفية فقط، ثم إعادة تركيب الجسم الحاد بنسبة 100% فوق الخلفية المموهة.
2. **المكتبات المشاركة:**
   * `@imgly/background-removal` (المتصفح).
   * `@imgly/background-removal-node` (السيرفر).
   * `sharp` (معالجة السيرفر وتقطيع ومزج الصور).
   * `@xenova/transformers` (بنية تحتية لتشغيل نماذج التعلم العميق).
3. **الملفات المسؤولة:**
   * `server/ai-background-removal.ts`
   * `client/src/lib/ai-background-removal.ts`
   * `client/src/lib/subject-extractor.ts`
   * `client/src/lib/subject-extractor.test.ts`

---

### تاسعاً: نظام الطبقات المتقدم والسجل (Layers & Non-Destructive History)
1. **التقنيات المستخدمة:**
   * **معمارية الطبقات المنفصلة (Layer Stack Compositing):** تمثيل كل طبقة ككائن مستقل يحمل كود التعريف `id`، الاسم، الشفافية، حالة القفل، حالة الظهور، ووضع المزج (Blend Mode).
   * **أوضاع المزج الرياضية (Blend Modes):**
     * Normal, Multiply, Screen, Overlay, Darken, Lighten, Color Dodge, Color Burn, Hard Light, Soft Light, Difference, Exclusion.
   * **نمط التراجع غير التدميري (Memento Pattern):** حفظ لقطات كاملة أو فارقة لحالة الكانفاس في شجرة سجل تتيح التراجع غير المحدود (`Ctrl+Z`) والإعادة (`Ctrl+Y`) وإنشاء نقاط استعادة مسماة.
2. **المكتبات المشاركة:** `nanoid` لتوليد معرفات الطبقات والخطوات الفريدة.
3. **الملفات المسؤولة:**
   * `client/src/lib/layers-history.ts`
   * `client/src/lib/phase5-layers.test.ts`

---

### عاشراً: محرك النصوص والطباعة (Typography Engine)
1. **التقنيات المستخدمة:**
   * **Canvas Text Rendering Pipeline:** استخدام دوال `fillText` و `strokeText` مع ضبط مصفوفات التحويل الزاوي (Rotation Matrix) ومقاييس الخطوط (Font Metrics).
   * **الخطوط العربية المتوافقة:** ربط مباشر مع خطوط ويب Google Fonts وتفعيل خاصية الاتصال الحرفي الصحيح للغة العربية داخل الـ Canvas.
   * **المؤثرات النصية:** الظلال متعددة الطبقات (Multi-pass Shadows)، التوهج الخارجي (Glow)، والتدرجات اللونية (Linear/Radial Text Gradients).
2. **الملفات المسؤولة:**
   * `client/src/pages/Home.tsx` (قسم إدارة عناصر النصوص).

---

### حادي عشر: محرك إدارة المشاريع والتصدير (Persistence & Export Engine)
1. **التقنيات المستخدمة:**
   * **صيغة المشروع المخصصة (`.imagepro`):** معمارية حزم JSON تجمع الصورة الأصلية بصيغة Base64، ومصفوفة الطبقات، وجميع عناصر الرسم والأشكال والنصوص، ومصفوفة التعديلات الضوئية، مع التحقق من صحة البيانات عبر مكتبة **Zod**.
   * **محرك التصدير متعدد المقاييس (Resampling Scaler):**
     * إنشاء كانفاس افتراضي مؤقت بالأبعاد المضاعفة (0.5×، 1.0×، 2.0× HD).
     * إعادة رسم وتصيير كافة المتجهات والنصوص بدقتها الحسابية الأصلية لمنع التشويه البكسلي.
     * التصدير بصيغ متعددة: `image/png`، `image/jpeg` (مع التحكم في معامل الجودة 1–100)، و `image/webp`.
2. **المكتبات المشاركة:** `zod`، واجهات المتصفح `URL.createObjectURL` و `Blob`.
3. **الملفات المسؤولة:**
   * `client/src/lib/project-persistence.ts`
   * `client/src/lib/project-persistence.test.ts`

---

### ثاني عشر: خادم الواجهة الخلفية والشبكة (Backend & API Server)
1. **التقنيات المستخدمة:**
   * **Node.js (v18/v20+) مع Express 4.21:** خادم خفيف وعالي الأداء يدعم بروتوكول HTTP ومعالجة حزم البيانات الكبيرة (توسيع حد حمولة JSON إلى 50MB لدعم نقل الصور فائقة الدقة).
   * **نقطة النهاية الذكية (`POST /api/remove-background`):** استقبال الصورة المشفرة، تمريرها لمكتبة **Sharp** لتقليم وتحديد منطقة الاهتمام (ROI)، ثم تشغيل نموذج **IS-Net** العصبي وإرجاع النتيجة بصيغة PNG شفافة فورية.
   * **خدمة الملفات الثابتة (Static File Server):** خدمة ملفات الواجهة المبنية في مجلد `dist/public` وإعادة توجيه كافة المسارات لـ `index.html` (Single Page Application Fallback).
2. **المكتبات المشاركة:** `express`, `sharp`, `@imgly/background-removal-node`, `axios`, `esbuild`.
3. **الملفات المسؤولة:**
   * `server/index.ts`
   * `server/ai-background-removal.ts`

---

### ثالث عشر: بيئة الاختبارات وضمان الجودة (Testing & Quality Assurance)
1. **التقنيات المستخدمة:**
   * **Vitest 2.1:** منصة اختبارات حديثة فائقة السرعة ومتوافقة مع نظام Vite Module Graph.
   * **الاختبارات الآلية المنفذة:** تغطية كاملة لجميع المحركات عبر 8 ملفات اختبارات و 37 فحص وحدة (Unit Tests) تعمل بنسبة نجاح 100%.
   * **فحص الأنواع (Type Checking):** تشغيل `tsc --noEmit` لضمان خلو كامل الكود من أي أخطاء برمجية أو تجاوزات في الأنواع.
2. **المكتبات المشاركة:** `vitest`, `typescript`.
3. **ملفات الاختبارات:**
   * `client/src/lib/filters-engine.test.ts`
   * `client/src/lib/retouch-engine.test.ts`
   * `client/src/lib/drawing-engine.test.ts`
   * `client/src/lib/color-adjustments.test.ts`
   * `client/src/lib/selection.test.ts`
   * `client/src/lib/subject-extractor.test.ts`
   * `client/src/lib/phase5-layers.test.ts`
   * `client/src/lib/tool-contract.test.ts`

---

## 5. المقارنة التقنية بين المعالجة في المتصفح وعلى الخادم
يوضح الجدول التالي الاستراتيجية الهندسية المتبعة في توزيع أعباء المعالجة داخل النظام:

| المعيار | المعالجة في المتصفح (Client-Side) | المعالجة على الخادم (Server-Side) |
|---|---|---|
| **المهام المنفذة** | • الفلاتر الـ 21 والالتفاف الرياضي<br>• التعديلات اللونية والهستوجرام الحي<br>• محرك الرسم والأشكال الهندسية<br>• أدوات التنقيح (ختم، معالجة، بقع)<br>• إدارة الطبقات والتصدير المباشر | • الذكاء الاصطناعي الثقيل (IS-Net C++)<br>• تقطيع ومعالجة مناطق الاهتمام (ROI)<br>• التعامل مع الصور الضخمة ومكتبة Sharp<br>• خدمة ملفات النظام وواجهات الـ API |
| **التقنيات المستعملة** | HTML5 Canvas 2D, TypedArrays, Web Workers, WASM | Node.js, Express, Sharp (libvips), ONNX Runtime |
| **زمن الاستجابة** | فوري (1–16 ميلي ثانية - 60 إلى 120 FPS) | 0.8 إلى 3 ثوانٍ لمعالجة الذكاء الاصطناعي |
| **استهلاك العتاد** | استغلال معالج الرسوميات والشاشات لدى العميل | استهلاك الذاكرة والأنوية المتعددة في السيرفر |
| **الاعتماد على الشبكة** | لا يحتاج إنترنت بعد فتح الصفحة | يحتاج اتصال محلي/شبكي لنقطة نهاية API |

---

## 6. الخاتمة والخلاصة الهندسية
يقدم نظام **ImagePro Studio** نموذجاً تطبيقياً حقيقياً يبرهن على إمكانية بناء برمجيات وتطبيقات ويب متقدمة لمعالجة الصور الرقمية تنافس البرامج المكتبية الكلاسيكية. تميز المشروع بالاعتماد على **خوارزميات رياضية أصيلة** تم بناؤها من الصفر لتنفيذ العمليات النقطية والالتفافية، مع توظيف متقن لأحدث أطر العمل ومكتبات واجهات المستخدم، ودمج سلس لشبكات الذكاء الاصطناعي العميقة.

---
> **تم إعداد هذا التقرير كتوثيق تقني وأكاديمي متكامل لمشروع التخرج.**  
> **فريق الإعداد والتطوير:**  
> • **سليمان العربي**  
> • **يعقوب المهاجري**  
> • **مالك عادل**  
