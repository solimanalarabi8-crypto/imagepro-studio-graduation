# 📋 التقرير التقني الشامل لنظام ImagePro Studio
### توثيق تفصيلي لكافة أجزاء النظام، المعمارية الهندسية، المكتبات البرمجية، ومحركات الذكاء الاصطناعي
> **مشروع تخرج لنيل درجة البكالوريوس في علوم الحاسوب وتكنولوجيا المعلومات**  
> **تخصص:** معالجة الصور الرقمية والرؤية الحاسوبية (Digital Image Processing & Computer Vision)  
> **إعداد الطلاب:**  
> • **سليمان العربي**  
> • **يعقوب المهاجري**  
> • **مالك عادل**  
> **تاريخ التوثيق المحدث:** 2026  

---

## 📑 فهرس التقرير
1. [المقدمة والملخص التنفيذي](#1-المقدمة-والملخص-التنفيذي)
2. [المعمارية العامة للنظام (System Architecture)](#2-المعمارية-العامة-للنظام-system-architecture)
3. [الجدول الشامل لكافة المكاتب والاعتماديات (Dependencies Inventory)](#3-الجدول-الشامل-لكافة-المكاتب-والاعتماديات-dependencies-inventory)
4. [تفصيل أجزاء النظام والتقنيات والمكتبات المستخدمة في كل جزء](#4-تفصيل-أجزاء-النظام-والتقنيات-والمكتبات-المستخدمة-في-كل-جزء)
   * [أولاً: بيئة العمل والبناء الأساسية (Core Runtime & Build System)](#أولاً-بيئة-العمل-والبناء-الأساسية-core-runtime--build-system)
   * [ثانياً: واجهة المستخدم وشريط الأوامر المزدوج (Two-Tier UI Architecture)](#ثانياً-واجهة-المستخدم-وشريط-الأوامر-المزدوج-two-tier-ui-architecture)
   * [ثالثاً: محرك الرسم البكسلي والكانفاس الأساسي (Canvas Core Engine)](#ثالثاً-محرك-الرسم-البكسلي-والكانفاس-الأساسي-canvas-core-engine)
   * [رابعاً: محرك الفلاتر والالتفاف الرياضي (Filters & 2D Convolution Engine)](#رابعاً-محرك-الفلاتر-والالتفاف-الرياضي-filters--2d-convolution-engine)
   * [خامساً: محرك الإضاءة والألوان والمدرج التكراري الحي (Color Engine & Live Histogram)](#خامساً-محرك-الإضاءة-والألوان-والمدرج-التكراري-الحي-color-engine--live-histogram)
   * [سادساً: محرك الرسم الرقمي والأشكال الهندسية (Drawing & Shapes Engine)](#سادساً-محرك-الرسم-الرقمي-والأشكال-الهندسية-drawing--shapes-engine)
   * [سابعاً: حزمة التنقيح وإزالة العيوب (Retouching & Healing Engine)](#سابعاً-حزمة-التنقيح-وإزالة-العيوب-retouching--healing-engine)
   * [ثامناً: استوديو الذكاء الاصطناعي والشبكات العصبية (AI Neural Engine & IS-Net)](#ثامناً-استوديو-الذكاء-الاصطناعي-والشبكات-العصبية-ai-neural-engine--is-net)
   * [تاسعاً: محرك القوالب والتصاميم متعددة الطبقات (Editable Templates Engine)](#تاسعاً-محرك-القوالب-والتصاميم-متعددة-الطبقات-editable-templates-engine)
   * [عاشراً: مكتبة الأصول والرسومات المتجهة وصور الستوك (Assets & Stock Photos Hub)](#عاشراً-مكتبة-الأصول-والرسومات-المتجهة-وصور-الستوك-assets--stock-photos-hub)
   * [حادي عشر: محرك المساطر التفاعلية والمحاذاة الذكية (Rulers & Smart Snap Engine)](#حادي-عشر-محرك-المساطر-التفاعلية-والمحاذاة-الذكية-rulers--smart-snap-engine)
   * [ثاني عشر: نظام الطبقات المتقدم والأقنعة (Layers Architecture & Masks)](#ثاني-عشر-نظام-الطبقات-المتقدم-والأقنعة-layers-architecture--masks)
   * [ثالث عشر: محرك النصوص والطباعة (Typography Engine)](#ثالث-عشر-محرك-النصوص-والطباعة-typography-engine)
   * [رابع عشر: نظام الحفظ التلقائي وإدارة المشاريع وسجل الإصدارات (Autosave & Projects Dashboard)](#رابع-عشر-نظام-الحفظ-التلقائي-وإدارة-المشاريع-وسجل-الإصدارات-autosave--projects-dashboard)
   * [خامس عشر: استوديو العلامة المائية ومحرك التصدير متعدد المقاسات (Watermark & Multi-Size Export)](#خامس-عشر-استوديو-العلامة-المائية-ومحرك-التصدير-متعدد-المقاسات-watermark--multi-size-export)
   * [سادس عشر: خادم الواجهة الخلفية والشبكة (Backend Server & AI Middleware)](#سادس-عشر-خادم-الواجهة-الخلفية-والشبكة-backend-server--ai-middleware)
   * [سابع عشر: بيئة الاختبارات وضمان الجودة (Testing & Quality Assurance - 70 Tests)](#سابع-عشر-بيئة-الاختبارات-وضمان-الجودة-testing--quality-assurance---70-tests)
5. [المقارنة التقنية بين المعالجة في المتصفح وعلى الخادم](#5-المقارنة-التقنية-بين-المعالجة-في-المتصفح-وعلى-الخادم)
6. [الخاتمة والخلاصة الهندسية](#6-الخاتمة-والخلاصة-الهندسية)

---

## 1. المقدمة والملخص التنفيذي
نظام **ImagePro Studio** هو منصة برمجية هندسية متكاملة لتحرير ومعالجة الصور الرقمية وتطبيق خوارزميات الرؤية الحاسوبية والذكاء الاصطناعي مباشرة عبر الويب. تم بناء النظام ليعمل بكفاءة فائقة تعتمد على المعالجة الرياضية النقطية المباشرة (Pixel-level manipulation) دون الاعتماد على مكاتب تجارية خارجية مغلقة لتطبيق المؤثرات الأساسية، بل تم بناء محركات معالجة رياضية مخصصة من الصفر لكل من الفلاتر، التعديلات الضوئية، الرسم، التنقيح، والمحاذاة.

يتكامل النظام مع شبكات عصبية عميقة (Deep Neural Networks) لمعالجة المهام المعقدة كالتجزئة الدلالية وفصل العناصر (Image Matting) عبر نموذج **IS-Net** العصبي، مع بنية تحتية سحابية محلية هجينة، ومكتبات تصميم عصرية تنافس منصات رائدة مثل Canva وPixelora.

---

## 2. المعمارية العامة للنظام (System Architecture)
يعتمد النظام على **معمارية هجينة متعددة الطبقات (Hybrid Multi-Tier Architecture)** تضمن أعلى سرعة استجابة للمستخدم مع توفير أقصى قوة حسابية:

```
+-----------------------------------------------------------------------------------+
|                        1. طبقة واجهة المستخدم وتجربة الاستخدام                     |
|           Two-Tier Command Bar (System Tier 1 + Studio Hero Ribbon Tier 2)        |
|                 React 19 + Tailwind CSS v4 + Radix UI + Lucide                    |
+-----------------------------------------------------------------------------------+
                                         │
                                         ▼
+-----------------------------------------------------------------------------------+
|                     2. طبقة إدارة الحالة وتدفق البيانات (State)                   |
|     Canvas Context + Tool Registry + Layers Stack + Snap Engine + History Memento |
+-----------------------------------------------------------------------------------+
        │                                                     │
        ▼                                                     ▼
+------------------------------------+   +------------------------------------------+
|  3. محركات المعالجة المحلية للعميل |   |     4. الواجهة الخلفية والذكاء الاصطناعي |
|     (Client-side Web Engines)      |   |            (Node.js / Express API)       |
| • محرك الكانفاس (HTML5 Canvas 2D)  |   | • خادم Express 4.21                      |
| • محرك الفلاتر (Convolution 3x3)   |   | • مكتبة Sharp (C++ Libvips)              |
| • محرك الإضاءة والهستوجرام الحسابي |   | • نموذج IS-Net العصبي (ONNX Runtime)     |
| • محرك التنعيم التربيعي للفرشاة    |   | • إزالة الخلفية الذكية ومناطق ROI        |
| • خوارزميات التنقيح (Bilateral/IDW)|   | • حماية ملفات النماذج عبر .gitattributes |
| • محرك المساطر والمغنطة الذكية     |   |                                          |
+------------------------------------+   +------------------------------------------+
                                         │
                                         ▼
+-----------------------------------------------------------------------------------+
|            5. طبقة التخزين وإدارة المشاريع (Persistence & Export Engine)          |
| Autosave Manager (Local Storage) + Version History + .imagepro Bundles + ZIP Exp  |
+-----------------------------------------------------------------------------------+
```

---

## 3. الجدول الشامل لكافة المكاتب والاعتماديات (Dependencies Inventory)

### أ. مكاتب التشغيل الأساسية (Production Dependencies)
| المكتبة (Library) | الإصدار | التصنيف | الدور الهندسي داخل النظام |
|---|:---:|:---:|---|
| **`react`** | `^19.2.1` | Core Framework | إطار العمل الأساسي لبناء واجهة المستخدم وتحديث شجرة العناصر التفاعلية. |
| **`react-dom`** | `^19.2.1` | Core Framework | ربط مكونات React بشجرة DOM الحقيقية للمتصفح. |
| **`@imgly/background-removal`** | `^1.7.0` | AI / Computer Vision | تشغيل نموذج الذكاء الاصطناعي IS-Net في المتصفح عبر WebAssembly و Web Workers. |
| **`@imgly/background-removal-node`** | `^1.4.5` | AI / Computer Vision | تشغيل نموذج الذكاء الاصطناعي على السيرفر بالاعتماد على محرك ONNX Runtime C++. |
| **`sharp`** | `^0.35.4` | High-Perf Image Processing | مكتبة C++ مدمجة بالسيرفر لمعالجة وتدوير وتقطيع ومزج الصور وإدارة مناطق ROI بسرعة فائقة. |
| **`@xenova/transformers`** | `^2.17.2` | Machine Learning | بنية تشغيل نماذج Hugging Face ومحولات الرؤية الحاسوبية في بيئة Node و JavaScript. |
| **`@google/genai`** | `^2.22.0` | AI Integration | واجهة برمجية للربط مع نماذج الذكاء الاصطناعي التوليدي عند الحاجة. |
| **`express`** | `^4.21.2` | Backend Server | خادم الويب الأساسي لخدمة واجهات برمجة التطبيقات (API) ونقطة `/api/remove-background`. |
| **`axios`** | `^1.12.0` | Networking | إدارة طلبات HTTP بين العميل وخادم المعالجة لنقل الصور والبيانات المعالجة. |
| **`lucide-react`** | `^0.453.0` | UI Icons | توفير كافة الأيقونات المتجهة لأدوات النظام، القوائم، والأزرار. |
| **`framer-motion`** | `^12.23.22` | UI Animation | تحريك اللوحات والنوافذ المنبثقة وانتقالات الحالة بسلاسة فيزيائية 60FPS. |
| **`recharts`** | `^2.15.2` | Data Visualization | رسم بياني عالي الدقة لعرض الهيستوجرام وتوزيع قنوات الألوان (R, G, B, Luminance). |
| **`sonner`** | `^2.0.7` | UI Notifications | نظام الإشعارات والتنبيهات المباشرة (Toast) لإعلام المستخدم بنجاح العمليات. |
| **`wouter`** | `^3.3.5` | Routing | موجه خفيف وسريع جداً لإدارة مسارات وتوجيه صفحات التطبيق. |
| **`zod`** | `^4.1.12` | Validation | فحص وتدقيق وتأكيد صحة البيانات وأنماط كائنات المشاريع وملفات الإعدادات. |
| **`clsx`** | `^2.1.1` | Styling Utility | دمج وتطبيق فئات CSS الشرطية بكفاءة عالية. |
| **`tailwind-merge`** | `^3.3.1` | Styling Utility | حل تعارضات فئات Tailwind CSS تلقائياً عند دمج الأنماط البرمجية. |

---

## 4. تفصيل أجزاء النظام والتقنيات والمكتبات المستخدمة في كل جزء

### أولاً: بيئة العمل والبناء الأساسية (Core Runtime & Build System)
* **Vite 7.1.7:** محرك بناء وخادم تطوير فائق السرعة يعتمد على وحدات ES Modules الأصلية (Native ESM) مع زمن استجابة وتحديث لحظي (Hot Module Replacement - HMR) أقل من 100 ميلي ثانية.
* **TypeScript 5.6.3:** لغة التطوير الأساسية لضمان الأمان البرمجي الكامل للأنواع (Strict Static Typing).
* **Tailwind CSS v4:** محرك التنسيق المالي مع المعمارية الرسومية الداكنة المستوحاة من غرف المونتاج.

---

### ثانياً: واجهة المستخدم وشريط الأوامر المزدوج (Two-Tier UI Architecture)
1. **الطبقة الأولى (Tier 1 - شريط النظام):**
   * يحمل هوية وشعار الاستوديو والقوائم الرئيسية (ملف، تعديل، صورة، طبقة، عرض، مساعدة).
   * محرك البحث السريع ولوحة الأوامر الفورية (Command Palette - `Ctrl + K`) المعتمد على خوارزميات المطابقة التقريبية.
   * تم تحسين الشريط وإزالة الأزرار الزائدة والمكررة لمنح واجهة نظيفة واحترافية.
2. **الطبقة الثانية (Tier 2 - شريط استوديو الإبداع Hero Ribbon):**
   * شريط كبسولي عائم يضم الأزرار الإبداعية الموحدة:
     * **`＋ تصميم جديد`:** كبسولة فيروزية زرقاء مخصصة لبدء المشاريع بالمقاسات الجاهزة.
     * **المكتبة الإبداعية:** hub شامل للأصول والخلفيات والقوالب.
     * **قوالب جاهزة:** استعراض القوالب متعددة الطبقات.
     * **استوديو الخلفيات:** تصوير المنتجات والبورتريه.
     * **استوديو الفلاتر:** استعراض المرشحات بالمعاينة الحية.
     * **استوديو الدمج:** مزج صورتين مع أوضاع الشفافية.
     * **مشاريعي:** لوحة إدارة المشاريع وسجل النسخ.
3. **إعادة هندسة الشريط الجانبي (Properties & Layers Coexistence):**
   * تم نقل أزرار الاستوديو السريعة (عزل بالذكاء الاصطناعي، دمج صورتين، العلامة المائية) إلى داخل لوحة الخصائص (`properties-panel`).
   * أتاح هذا التعديل تحرير لوحة الطبقات (`layers-panel`) لتظهر بكامل ارتفاعها دون أن تحجب أزرار الاستوديو أي طبقة من طبقات المستخدم.

---

### ثالثاً: محرك الرسم البكسلي والكانفاس الأساسي (Canvas Core Engine)
1. **التقنيات المستخدمة:**
   * **HTML5 Canvas 2D Context API:** الرسم المباشر والوصول للبكسلات عبر كائنات `ImageData`.
   * **مصفوفات البكسل المباشرة (Uint8ClampedArray):** معالجة مصفوفة البكسلات أحادية البعد ذات النمط `[R, G, B, A, ...]`.
   * **المصفوفات الرياضية ثنائية الأبعاد:** تحويل الإحداثيات من فضاء الشاشة (Screen Coordinates) إلى فضاء الصورة الحقيقي (Image Coordinates) وفق مصفوفة التحويل الأفيني (Affine Transform Matrix) المتأثرة بنسبة التكبير والإزاحة.
2. **أداة القص التفاعلية المطورة (Enhanced Crop Tool):**
   * **صندوق القص التلقائي:** حساب أبعاد 90% تلقائياً من الصورة وتوسيطها عند اختيار أداة القص (`C`).
   * **شبكة قاعدة الأثلاث (Rule of Thirds Grid):** تقسيم مساحة القص بخطوط أفقية وعمودية عند $\frac{1}{3}$ و $\frac{2}{3}$ لموازنة التكوين الفوتوغرافي.
   * **بادج الأبعاد اللحظي (HUD Badge):** مكون بصري يطفو فوق مقبض القص يعرض لحظياً أبعاد العرض والارتفاع والإحداثيات.
   * **معالجة الاختصارات:** ربط مفتاح `Enter` لتنفيذ القص ومفتاح `Esc` للإلغاء.

---

### رابعاً: محرك الفلاتر والالتفاف الرياضي (Filters & 2D Convolution Engine)
1. **التقنيات والخوارزميات المستخدمة:**
   * **خوارزمية الالتفاف المكاني (Spatial 2D Convolution):** تطبيق نوى رياضية بمصفوفات $3 \times 3$ و $5 \times 5$ لحساب القيم الجديدة لكل بكسل بناءً على جيرانه.
   * **فلاتر كشف الحواف:** فلتر سوبل (Sobel) عبر حساب التدرج الرياضي $G = \sqrt{G_x^2 + G_y^2}$، فلتر كاني (Canny)، لابلارسيان (Laplacian)، والنقش البارز (Emboss).
   * **فلاتر التمويه والحدة:** التمويه الجاوسي (Gaussian Blur)، تمويه الحركة (Motion Blur)، وقناع الحدة المجهري (Unsharp Masking).
   * **الفلاتر الفنية والهندسية:** المرشح الوسيط (Median Filter) لإزالة شوائب الملح والفلفل، التنعيم الثنائي (Bilateral Filter)، البكسلة (Pixelate)، الملصق النغمي (Posterize)، واللوحة الزيتية (Oil Painting).
2. **الملفات المسؤولة:**
   * `client/src/lib/filters-engine.ts`
   * `client/src/lib/filters-engine.test.ts` (5 اختبارات وحدة شاملة ناجحة).

---

### خامساً: محرك الإضاءة والألوان والمدرج التكراري الحي (Color Engine & Live Histogram)
1. **التقنيات والخوارزميات:**
   * **معادلات التباين والسطوع الخطية:** $C' = (C - 128) \times \text{factor} + 128 + \text{brightness}$.
   * **فضاء الألوان HSL:** التحويل بين RGB و HSL لتدوير زاوية اللون (Hue) وضبط التشبع بدقة رياضية.
   * **تصحيح جاما (Gamma Correction):** معادلة أسية غير خطية $V_{\text{out}} = 255 \times (V_{\text{in}} / 255)^\gamma$.
   * **المدرج التكراري الحي (Single-Pass Real-Time Histogram):** قراءة مصفوفة البكسلات في دورة أحادية وحساب التوزيع التكراري لـ 256 مستوى لكل قناة (Red, Green, Blue, Luminance) وتحديثها فورياً بالاعتماد على `recharts`.
2. **الملفات المسؤولة:**
   * `client/src/lib/color-adjustments.ts`
   * `client/src/lib/color-adjustments.test.ts` (3 اختبارات وحدة ناجحة).

---

### سادساً: محرك الرسم الرقمي والأشكال الهندسية (Drawing & Shapes Engine)
1. **التقنيات المستخدمة:**
   * **التنعيم بالمنحنيات التربيعية (Quadratic Bezier Midpoint Smoothing):** التقاط نقاط حركة الفأرة المتتالية وحساب نقاط المنتصف ورسم منحنيات تربيعية ناعمة بينها لإلغاء الرعشة اليدوية.
   * **الملء الفيضاني (BFS Flood Fill):** خوارزمية البحث في العرض رباعية الاتجاهات مع حساب المسافة الإقليدية في فضاء RGB لمطابقة التفاوت اللوني (Tolerance).
   * **محرك المتجهات للأشكال:** رسم الأشكال الهندسية (مستطيل، بيضاوي، خط، مثلث، نجمة) مع دعم أنماط الإطار والتعبئة.
2. **الملفات المسؤولة:**
   * `client/src/lib/drawing-engine.ts`
   * `client/src/lib/drawing-engine.test.ts` (3 اختبارات وحدة ناجحة).

---

### سابعاً: حزمة التنقيح وإزالة العيوب (Retouching & Healing Engine)
1. **التقنيات والخوارزميات:**
   * **ختم الاستنساخ (Clone Stamp):** نسخ بكسلي متدرج مع دالة ترييش جيب التمام الناعمة (Cosine Falloff Weighting).
   * **فرشاة المعالجة (Healing Brush):** استخراج النسيج عالي التردد من المصدر ومزجه مع إضاءة ولون المنطقة الهدف.
   * **معالجة البقع الفورية (Spot Healing):** أخذ عينات دائرية ونشرها بنظام مقلوب مربع المسافة (IDW).
   * **إزالة العين الحمراء (Red-Eye Removal):** كشف وقمع القناة الحمراء مع صيانة بريق العين المنعكس.
   * **تنعيم البشرة الذكي (Smart Skin Smoothing):** فلتر ثنائي الاتجاه محصور بنطاق درجات ألوان البشرة (Skin Gamut).
   * **التعبئة المحيطية الذكية (Inpaint Fill):** نشر أنسجة الإطار المحيط المتصل لملء المساحات المفرغة.
2. **الملفات المسؤولة:**
   * `client/src/lib/retouch-engine.ts`
   * `client/src/lib/retouch-engine.test.ts` (7 اختبارات وحدة ناجحة).

---

### ثامناً: استوديو الذكاء الاصطناعي والشبكات العصبية (AI Neural Engine & IS-Net)
1. **التقنيات ونماذج التعلم العميق:**
   * **نموذج IS-Net (Dichotomous Image Segmentation):** شبكة عصبية تلافيفية متخصصة في تجزئة العناصر وتوليد أقنعة ألفا فائقة النعومة للشعر والأقمشة.
   * **معمارية المعالجة المزدوجة (Hybrid Dual Architecture):**
     * معالجة عميل عبر المتصفح باستخدام WASM و Web Workers.
     * معالجة سيرفر عبر محرك ONNX Runtime C++ Native و Sharp لضمان الدقة وتجاوز قيود المتصفحات.
   * **حل مشكلة النماذج الثنائية وحمايتها بـ `.gitattributes`:**
     * تم إنشاء ملف `.gitattributes` وتثبيت خاصية `client/public/imgly-data/* binary` لمنع نظام Git على بيئة Windows من تحويل نهايات الأسطر (CRLF Conversion) الذي كان يتسبب في تضخم ملف النموذج وتعطيل استجابة API.
   * **مزايا الاستوديو المحدثة:**
     * **عزل المحتوى الصافي فوراً (Pure Content Cutout):** استخراج العنصر وتفريغه وجعله حراً ومستقلاً مع لوحة تحكم سريعة (تكبير، تصغير، محاذاة، واقتصاص مساحة العمل).
     * **تمويه البورتريه الحقيقي (True Portrait Bokeh):** تمويه الخلفية بنعومة واقعية مع الحفاظ الصارم على حدة وحدة ونقاء ألوان الجسم بنسبة 100%.
     * **استبدال الخلفيات الفوري:** شفاف، أسود استوديو، أبيض نقي، كروما خضراء، وتدرج سينمائي داكن.
     * **رفع صورة خلفية مخصصة:** تركيب أي صورة من جهاز العميل خلف العنصر المفرغ.
2. **الملفات المسؤولة:**
   * `server/ai-background-removal.ts`
   * `client/src/lib/subject-extractor.ts`
   * `client/src/lib/subject-extractor.test.ts` (6 اختبارات وحدة ناجحة).

---

### تاسعاً: محرك القوالب والتصاميم متعددة الطبقات (Editable Templates Engine)
1. **المعمارية الهندسية:**
   * دعم القوالب التصميمية المركبة بجميع طبقاتها (خلفيات، متجهات، نصوص، وشعارات).
   * تصنيفات شاملة تشمل: `social`, `instagram`, `facebook`, `tiktok`, `youtube`, `linkedin`, `presentations`, `business`, `marketing`, `education`, `cv`, `invitations`, `posters`, `flyers`, `ads`, `ecommerce`.
2. **الملفات المسؤولة:**
   * `client/src/lib/editable-templates.ts`
   * `client/src/lib/editable-templates.test.ts` (2 اختباري وحدة ناجحة).

---

### عاشراً: مكتبة الأصول والرسومات المتجهة وصور الستوك (Assets & Stock Photos Hub)
1. **المعمارية الهندسية:**
   * **مكتبة الأصول المتجهة (Vector Assets):** مئات العناصر المصنفة (أيقونات، أشكال، شارات، ملصقات، إطارات، أسهم، وزخارف) المرسومة بدوال Canvas برمجية خالصة دون أي صور نقطية ثقيلة.
   * **مكتبة صور الستوك (Stock Photos):** صور عالية الجودة مصنفة حسب الموضوعات.
   * **مركز السحب والإفلات الذكي (Smart Drop Hub):** التقاط السحب والإفلات وتوجيه الأصول كطبقات أو كخلفيات مستبدلة.
2. **الملفات المسؤولة:**
   * `client/src/lib/assets-library.ts`
   * `client/src/lib/stock-photos-library.ts`
   * `client/src/lib/smart-drop-hub.ts`
   * `client/src/lib/smart-drop-hub.test.ts` (5 اختبارات وحدة ناجحة).

---

### حادي عشر: محرك المساطر التفاعلية والمحاذاة الذكية (Rulers & Smart Snap Engine)
1. **المعمارية الهندسية:**
   * **Interactive Rulers (`Ctrl + R`):** مساطر أفقية وعمودية تحسب فضاء الإحداثيات الحقيقي المتأثر بالـ Zoom والـ Pan.
   * **Draggable Guides:** خطوط إرشادية ديناميكية تسحب من المساطر وتثبت في أي موقع بكسلي.
   * **Smart Snap System:** حساب المسافة المتعامدة بين مراكز وحواف العناصر وخطوط الإرشاد ومنتصف الكانفاس والمغنطة التلقائية عند الاقتراب ضمن نطاق التسامح ($\le 6\text{px}$).
2. **الملفات المسؤولة:**
   * `client/src/lib/guides-snap-engine.ts`
   * `client/src/lib/guides-snap-engine.test.ts` (4 اختبارات وحدة ناجحة).

---

### ثاني عشر: نظام الطبقات المتقدم والأقنعة (Layers Architecture & Masks)
1. **المعمارية الهندسية:**
   * **Layer Stack Compositing:** بنية طبقات مستقلة غير تدميرية.
   * **Layer Masks:** قناع ألفا مستقل لكل طبقة يتيح الإخفاء والإظهار دون إتلاف بكسلات الرسم.
   * **أوضاع المزج (Blend Modes):** Normal, Multiply, Screen, Overlay, Darken, Lighten, Color Dodge, Color Burn, Hard Light, Soft Light, Difference.
   * **وضع العزل الفردي (Solo Mode):** إظهار الطبقة المحددة بمفردها لتنقيح تفاصيلها الدقيقة.
2. **الملفات المسؤولة:**
   * `client/src/lib/layers-history.ts`
   * `client/src/lib/phase5-layers.test.ts` (14 اختبار وحدة شامل ناجح).

---

### ثالث عشر: محرك النصوص والطباعة (Typography Engine)
1. **المعمارية الهندسية:**
   * رسم النصوص المتجهية عبر Canvas 2D مع دعم الاتصال الحرفي للخطوط العربية.
   * خطوط عربية مدمجة (Cairo, Tajawal, Amiri, IBM Plex Sans Arabic, Changa) وخطوط لاتينية عصرية (Inter, Roboto, Space Grotesk, Montserrat).
   * مؤثرات متقدمة: الظلال متعددة الطبقات (Multi-pass Shadows)، التوهج الخارجي (Glow)، والحدود (Stroke).

---

### رابع عشر: نظام الحفظ التلقائي وإدارة المشاريع وسجل الإصدارات (Autosave & Projects Dashboard)
1. **المعمارية الهندسية:**
   * **Autosave Manager:** تخزين محلي تلقائي دوري كل 30 ثانية في `localStorage` و `IndexedDB` يضمن سلامة الأعمال واستعادتها الفورية.
   * **Projects Dashboard Modal:** لوحة تحكم كاملة للمشاريع المحفوظة (عرض مصغر، أبعاد، تاريخ، تكرار Duplicate، وسلة مهملات Trash & Restore).
   * **Version History:** سجل نقاط استعادة زمني تفصيلي للمشروع مع دعم الملاحظات والوصف لكل إصدار.
2. **الملفات المسؤولة:**
   * `client/src/lib/autosave-manager.ts`
   * `client/src/lib/autosave-manager.test.ts` (3 اختبارات وحدة ناجحة).
   * `client/src/components/ProjectsDashboardModal.tsx`
   * `client/src/components/VersionHistoryModal.tsx`

---

### خامس عشر: استوديو العلامة المائية ومحرك التصدير متعدد المقاسات (Watermark & Multi-Size Export)
1. **استوديو العلامة المائية (Watermark Studio):**
   * دمج شعار مرئي أو نص حقوقي مخصص مع التحكم في الحجم، الشفافية، والنمط الشبكي المائل (Tile Pattern).
2. **محرك التصدير متعدد المقاسات (Multi-Size Export Hub):**
   * خوارزمية إعادة تحجيم متوازية تنشئ نسخاً مخصصة لجميع مقاسات السوشيال ميديا (Post, Story, Cover, Banner, Thumbnail) وتجمعها في ملف `.zip` واحد.
3. **الملفات المسؤولة:**
   * `client/src/components/WatermarkModal.tsx`
   * `client/src/components/MultiSizeExportModal.tsx`

---

### سادس عشر: خادم الواجهة الخلفية والشبكة (Backend Server & AI Middleware)
1. **المعمارية الهندسية:**
   * خادم Node.js مع Express 4.21 يدعم حزم JSON بسعة 50MB لاستقبال الصور الخام.
   * نقطة النهاية `POST /api/remove-background`:
     * استقبال الصورة المشفرة Base64.
     * استخدام مكتبة **Sharp** لتقطيع مناطق الاهتمام (ROI) عند التحديد.
     * استدعاء محرك **IS-Net** العصبي عبر `@imgly/background-removal-node`.
     * إعادة النتيجة بصيغة PNG شفافة عالية الجودة مع توثيق زمن المعالجة (`elapsedMs`).
2. **الملفات المسؤولة:**
   * `server/index.ts`
   * `server/ai-background-removal.ts`

---

### سابع عشر: بيئة الاختبارات وضمان الجودة (Testing & Quality Assurance - 70 Tests)
يعتمد النظام على منصة **Vitest 2.1** المتكاملة، حيث تجتاز الشيفرة البرمجية **70 اختبار وحدة آلي عبر 16 ملف اختبار بنسبة نجاح 100%**:

| ملف الاختبار | موضوع الفحص والتحقق | عدد الاختبارات | النتيجة |
|---|---|:---:|:---:|
| `src/lib/phase5-layers.test.ts` | معمارية الطبقات، المزج، والأقنعة | 14 | ✅ نجاح 100% |
| `src/lib/retouch-engine.test.ts` | حزمة التنقيح (ختم، معالجة، بقع، تنعيم) | 7 | ✅ نجاح 100% |
| `src/lib/selection.test.ts` | محرك التحديد الهندسي والعمليات المنطقية | 7 | ✅ نجاح 100% |
| `src/lib/subject-extractor.test.ts` | استخراج الأجسام وتأثيرات الخلفية والبوكيه | 6 | ✅ نجاح 100% |
| `src/lib/filters-engine.test.ts` | خوارزميات الالتفاف والفلاتر الـ 21 | 5 | ✅ نجاح 100% |
| `src/lib/smart-drop-hub.test.ts` | مركز السحب والإفلات الذكي للأصول | 5 | ✅ نجاح 100% |
| `src/lib/guides-snap-engine.test.ts` | المساطر التفاعلية والمحاذاة والمغنطة | 4 | ✅ نجاح 100% |
| `src/lib/drawing-engine.test.ts` | محرك الفرشاة والانسيابية والملء الفيضاني | 3 | ✅ نجاح 100% |
| `src/lib/color-adjustments.test.ts` | التعديلات الضوئية والمدرج التكراري | 3 | ✅ نجاح 100% |
| `src/lib/project-persistence.test.ts` | حزم وتشفير ملفات المشاريع (.imagepro) | 3 | ✅ نجاح 100% |
| `src/lib/autosave-manager.test.ts` | الحفظ التلقائي المحلي وسجل المشاريع | 3 | ✅ نجاح 100% |
| `src/lib/creative-backgrounds.test.ts` | كتالوج الخلفيات الإبداعية الـ 17 فئة | 2 | ✅ نجاح 100% |
| `src/lib/product-backgrounds.test.ts` | استوديو خلفيات المنتجات وتصيير الكانفاس | 2 | ✅ نجاح 100% |
| `src/lib/editable-templates.test.ts` | القوالب الجاهزة متعددة الطبقات | 2 | ✅ نجاح 100% |
| `src/lib/templates.test.ts` | مقاسات التصاميم والمنصات الرقمية | 2 | ✅ نجاح 100% |
| `src/lib/tool-contract.test.ts` | تكامل عقود الأدوات ومزامنة الواجهة | 2 | ✅ نجاح 100% |
| **المجموع الكلي** | **تغطية شاملة لكافة محركات النظام** | **70** | **✅ 70/70 بنجاح تام** |

بالإضافة إلى ذلك، يتم تنفيذ فحص الأنواع الصارم (`tsc --noEmit`) محققاً **0 أخطاء برمجية** عبر كامل ملفات المشروع.

---

## 5. المقارنة التقنية بين المعالجة في المتصفح وعلى الخادم
| المعيار | المعالجة في المتصفح (Client-Side) | المعالجة على الخادم (Server-Side) |
|---|---|---|
| **المهام المنفذة** | • الفلاتر الـ 21 والالتفاف المكاني<br>• التعديلات اللونية والهستوجرام الحي<br>• محرك الرسم التنعيمي والأشكال<br>• أدوات التنقيح (ختم، معالجة، بقع، تبييض)<br>• إدارة الطبقات والأقنعة والمساطر<br>• الحفظ التلقائي المحلي وسجل الإصدارات | • استخراج الأجسام عبر شبكة IS-Net العصبية<br>• معالجة مناطق الاهتمام (ROI) بمكتبة Sharp<br>• معالجة الصور الضخمة وتجاوز قيود الرام<br>• خدمة ملفات النظام وواجهات API<br>• حفظ الملفات الثنائية وضمان سلامتها |
| **التقنيات المستعملة** | HTML5 Canvas 2D, TypedArrays, Web Workers, WASM | Node.js, Express, Sharp (libvips), ONNX Runtime C++ |
| **زمن الاستجابة** | لحظي (1–16 ميلي ثانية - 60 إلى 120 FPS) | 0.8 إلى 2.5 ثانية للشبكة العصبية العميقة |
| **استهلاك العتاد** | استغلال معالج الرسوميات والشاشة لدى العميل | استهلاك الذاكرة والأنوية المتعددة في السيرفر |
| **الاعتماد على الشبكة** | لا يتطلب إنترنت بعد تحميل الصفحة | يتطلب اتصالاً محلياً بنقطة نهاية API |

---

## 6. الخاتمة والخلاصة الهندسية
يقدم نظام **ImagePro Studio** نموذجاً تطبيقياً حقيقياً يبرهن على إمكانية بناء برمجيات وتطبيقات ويب متقدمة لمعالجة وتصميم الصور الرقمية تنافس البرامج المكتبية الكلاسيكية. تميز المشروع بالاعتماد على **خوارزميات رياضية أصيلة** تم بناؤها من الصفر لتنفيذ العمليات النقطية والالتفافية، مع توظيف متقن لأحدث أطر العمل ومكتبات واجهات المستخدم، ودمج سلس لشبكات الذكاء الاصطناعي العميقة، ونظام اختبارات وضمان جودة صارم يغطي 70 اختباراً آلياً بنسبة نجاح 100%.

---
> **تم إعداد هذا التقرير كتوثيق تقني وأكاديمي متكامل لمشروع التخرج.**  
> **فريق الإعداد والتطوير:**  
> • **سليمان العربي**  
> • **يعقوب المهاجري**  
> • **مالك عادل**  
