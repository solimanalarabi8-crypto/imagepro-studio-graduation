# مواصفات المرحلة العاشرة: الأقنعة (Layer Masks)

## الهدف
تقديم ميزة "الأقنعة" (Masks) غير التدميرية، والتي تتيح إخفاء أو إظهار أجزاء من أي طبقة دون حذف وحدات البكسل الأصلية نهائياً.

## التغييرات المطلوبة

### 1. تحديث نموذج البيانات (Data Types)
- تحديث نوع `LayerInfo` ليحتوي على الخصائص الجديدة:
  - `maskData?: string`: لتخزين صورة القناع (بالأبيض والأسود) بصيغة Base64.
  - `maskEnabled?: boolean`: للقدرة على تعطيل وتفعيل القناع مؤقتاً.
  - `maskLinked?: boolean`: لربط القناع بالطبقة عند التحريك.
- إضافة حالة `selectedTarget: "content" | "mask"` لتحديد بيئة الرسم.

### 2. محرك العرض (Render Engine)
- عند وجود `maskData` لطبقة معينة وتكون `maskEnabled !== false`، يتم رسم الطبقة على `Offscreen Canvas`.
- يُرسم القناع فوق الطبقة بصلاحية `destination-in`.
- تُرسم اللوحة المؤقتة على اللوحة الرئيسية للبرنامج.

### 3. واجهة شريط الطبقات (Layer Panel)
- زر جديد لإضافة القناع `Add Mask`.
- صورة مصغرة إضافية بجانب الطبقة لتمثيل القناع.
- إمكانية الضغط على القناع وتفعيل وضع الرسم على القناع.
- قائمة منسدلة للقناع (تعطيل، حذف، تطبيق).

### 4. أدوات الرسم والتعديل
- عند `selectedTarget === "mask"`.
- إجبار الممحاة والفرشاة على الرسم بتدرجات الأبيض والأسود لتحديث بيانات `maskData`.

## خطة الاختبار
- إنشاء طبقة جديدة، تطبيق القناع عليها.
- الرسم باللون الأسود لمسح أجزاء، والرسم بالأبيض لاستعادتها.
- تعطيل القناع للتأكد من الصورة الأصلية لم تتأثر.
